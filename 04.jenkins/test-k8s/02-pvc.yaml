apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: new-jenkins-pvc
  namespace: skala-practice
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: efs-sc-shared
