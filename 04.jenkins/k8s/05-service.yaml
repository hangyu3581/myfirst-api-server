apiVersion: v1
kind: Service
metadata:
  name: sk199-jenkins
  namespace: class-3
  labels:
    app: sk199-jenkins
spec:
  selector:
    app: sk199-jenkins
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
# Jenkins JNLP Agent 연결 전용 서비스
# Kubernetes 동적 Pod 에이전트가 컨트롤러에 접속하는 채널
apiVersion: v1
kind: Service
metadata:
  name: sk199-jenkins-agent
  namespace: class-3
  labels:
    app: sk199-jenkins
spec:
  selector:
    app: sk199-jenkins
  ports:
    - name: jnlp
      protocol: TCP
      port: 50000
      targetPort: 50000
  type: ClusterIP
