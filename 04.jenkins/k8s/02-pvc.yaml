apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sk199-jenkins-pvc
  namespace: class-3
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: efs-sc-shared
