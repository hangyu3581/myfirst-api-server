apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: sk067-myfirst-ingress
  namespace: class-3
spec:
  ingressClassName: public-nginx
  rules:
  - host: sk067-ingress.skala25a.project.skala-ai.com
    http:
      paths:
      - backend:
          service:
            name: sk067-custom-jenkins
            port:
              number: 8080
        path: /jenkins
        pathType: Prefix
      - backend:
          service:
            name: sk067-myfirst-api-server
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - 'sk067-ingress.skala25a.project.skala-ai.com'
    secretName: sk067-ingress-project-tls-cert
